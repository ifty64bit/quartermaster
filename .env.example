# Local DB commands (bun run db:*) load .env via dotenv. For the local dev
# server, Nitro reads from .env.local. For production, set these in the
# Vercel dashboard or via `vercel env add`.

TURSO_AUTH_TOKEN=""
TURSO_DATABASE_URL=""
DATABASE_URL=""
BETTER_AUTH_URL=""
BETTER_AUTH_SECRET=""
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""